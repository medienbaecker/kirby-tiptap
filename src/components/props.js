export const props = {
  label: String,
  value: String,
  placeholder: String,
  disabled: Boolean,
  required: Boolean,
  spellcheck: Boolean,
  help: String,
  minlength: Number,
  maxlength: Number,
  size: String,
  buttons: Array,
  highlights: Array,
  kirbytags: Array,
  endpoints: Object,
}
