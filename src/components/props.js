export const props = {
  label: String,
  value: String,
  placeholder: String,
  disabled: Boolean,
  spellcheck: Boolean,
  help: String,
  minlength: Number,
  maxlength: Number,
  size: String,
  buttons: Array,
  highlights: Array,
  kirbytags: Array,
}
